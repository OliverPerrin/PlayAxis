/* Redirect rules are now managed via netlify.toml. This file is intentionally left without rules.
	Keeping it as a comment-only file avoids VS Code CSS linter errors while preventing duplicate rules. */